import React from 'react';
import { movies } from '../data';

const Movies = () => {
  return (
    <div>
        <h1>Movies Page</h1>
        {console.log(props)}
    </div>
  );
};

export default Movies;
